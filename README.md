# Data-cleaning-project
This project demonstrates a real-world data cleaning workflow using Python and Pandas in a Jupyter Notebook. The dataset intentionally contains common data quality issues such as missing values, duplicate records, inconsistent data types, and malformed CSV structure.
